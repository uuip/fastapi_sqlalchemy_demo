apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-code-pvc
  namespace: honey
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 1Gi
